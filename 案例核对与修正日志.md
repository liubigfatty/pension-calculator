# 案例核对与修正日志

> 始于 2026-07-10。本文件是"拿真实核定表量引擎"的逐省账本，每步留痕、可回滚。

## 操作规则（大哥确认）
1. **外部标尺**：以大哥提供的**真实社保局核定表**为唯一权威，不以现有 cases 册当真理。
2. **三项分比**：每个案例分别比对 `基础养老金 / 个人账户养老金 / 过渡性养老金`，定位差异段落。
3. **逐步修正**：一个省 / 一个案例核对清楚再动下一个，不批量瞎改。
4. **留记录**：每步写进本文件 + 当日 memory 日志。
5. **可回滚**：每次实质修改前确保 git 有基线快照；改坏了用 `git revert <commit>` 或 `git checkout -- <file>` 回到基线。

## 回滚基线
- 基线 commit：见本文件创建时的 git 提交（下方"基线 hash"）。
- 基线时状态：
  - ✅ 黑龙江：已按大哥官方表（1992–2025）整本重写 `AVG_SALARY_HISTORY` 与 `PROV_BASE`，cases 7/7 通过。
  - ⚠️ 重庆：系数 0.013→0.014（官网确认），但 cases 本身口径混乱（同省混用 sight/pre/0 占位），未动引擎公式。
  - ❓ 吉/蒙/沪/晋/藏：`AVG` 为早期"相邻相等"修复（基于旧框架，可能需回退），待真实案例复核。
  - 其余省：原始值未动。

## 已核对清单（按大哥发来的真实案例逐行追加）
| 省 | 收到真实案例 | 引擎当前结论 | 核对结论 | 修正动作 | 回滚点(commit) | 备注 |
|---|---|---|---|---|---|---|
| （待填） |

## 方法论备忘（为什么不能信 cases 册）
- cases 册 31 省均不可全信：含早期引擎输出回填值（git 1269a37）、零覆盖（无中人→过渡=0 未测）、逻辑矛盾（sight=0 却过渡>0）。
- **"案例通过" ≠ "引擎正确"**：通过只代表 引擎 == expected，而 expected 可能脏。
- 只有真实核定表（四项金额官方打印）能当尺子。

## 基线 hash
（提交后由 git rev-parse HEAD 填入）
