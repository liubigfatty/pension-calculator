# 2026-07-07 工作归档

> 当日主题：审计整改收尾 + 官方数据校正 + 31 省配置副本全量同步 + 微信虚拟支付上线 + 网页版重建启动

## 一、总览

| 项 | 状态 |
|---|---|
| 微信虚拟支付 banned 根因定位与修复 | ✅ 已上线（用户今日重传 calculate 云函数后生效） |
| 整体审计整改（P0/P1/P2 共 122 项任务） | ✅ 全部完成 |
| 江西 / 甘肃官方数据校正 | ✅ 完成并提交 |
| 31 省×6 配置副本全量同步 | ✅ 完成（commit 57fbf2a） |
| 网页版重建 | 🚧 进行中（今日启动，见下文） |

本地 `main` 领先远程多个 commit（含今日 57fbf2a），因执行环境连不上 github.com **尚未 push**，待用户在能联网环境 `git push origin main`。

## 二、关键里程碑

### 1. 微信支付 banned（已解决）
- 根因：虚拟支付已开通（OfferID=1450568626），但代码用标准支付 `wx.requestPayment` 卖虚拟商品（养老金报告）→ 用错通道被风控拦截 `requestPayment:fail banned`。
- 修复链路：`f7487bf` 支付迁移到 `wx.requestVirtualPayment`（道具直购 `pension_report`，goodsPrice=100 分=后台 ¥1）；`bc264d3` 钉死 signData 字段类型（buyQuantity/env/goodsPrice=number，其余=string，单位=分）；`d3d9cab` 签名缺失防御；`40d2ced` 双基数省 base_rates 重复 key 隐患。
- 验证：真机支付成功；`check-vpay-signData.js` 10/10。

### 2. 报告页与分享图
- `13bbcbd` 报告页视觉重设计；`06f0828` 三处内容逻辑修复（提前退休期间误用计发月数、累计对比显式标注正常退休年龄、替代率动态文案）；`3df02a7` 屏显回归热修（A1/A2/B1）；`5cf3326` 保存图迁移原生 Canvas2D（C9）+ 屏显修复（C4）。
- `df3cfa3`/`fd666cb` 分享图 roundRect 报错的 radii 数组写法修复。

### 3. 审计整改（Task #101，全部清空）
- P0：A1/A2 报告屏显回归、A3 江西社平、A4 计发基数年份不同步、A5 末年÷12 单位错、A6 verify 不覆盖生产副本 → `3df02a7`/`42a3fb6` 修复。
- P1：B1 医保上屏、B2 ef50/fw50 弹性路径分支缺失（`9648a83`）、B3 支付取消 errno=-2、B4 服务端发货推送、B5 双份单位不一致、B6 重复 key 死代码。
- P2：C1-C9（支付/报告/分享图/配置/校验）。

### 4. 官方数据校正
- 江西（commit `1269a37`/`1419504`/`739281b`/`ed74502`/`6936725`）：按江西省人社厅/统计局官方口径，2020 起计发基数用「基本养老金月计发基数」、社平用「全口径城镇单位就业人员月平均工资」；1995-2019 非私营÷12。
- 甘肃（commit `ed74502`）：按甘肃省人社厅 1995-2025 完整官方数据校正，修正年份错位+缴费/计发基数混用。
- 吉林（commit `d3d9cab`/`739281b`）：修复 getEngineConfig 重复 key 致基础养老金=0；历史计发基数约 2-3 倍偏差全部校正。

### 5. 31 省配置副本全量同步（commit `57fbf2a`）
- 背景：审计整改与官方校正后，**仅江西**做了多副本同步，其余 30 省在 6 个副本位置间仍存在分叉（root 缺历史计发基数、data 社平空、js/docs 单位错、cf 内 json 脏）。
- 策略（用户确认「以 root 口径为准」）：root 近年份校正值优先 + 云函数 js 历史年份补全，再同步到全部副本。
- 结果：`compare_provinces` 扫描 **31 省×6 副本数据指纹分叉=0**；`verify.js` **172/172** 通过（hubei-wuhan-2022 因历史计发基数补全重标定 1810.75→1626.44）。
- 提交：187 文件（186 省份副本 + 1 案例）。

## 三、配置副本架构（重要认知，纠正旧误解）

实际 git 跟踪 **5 目录 6 副本位置**（非"双份"）：
- A. `provinces/*.json` — root，verify 校验基准（**不是网页**）
- B. `cloudfunctions/calculate/provinces/*.js` — **线上云函数实际读取 getEngineConfig()，生效位**
- C. `cloudfunctions/calculate/provinces/*.json` — 云函数内 json，脏副本，**不生效**
- D. `data/provinces/*.js` — 被 data/pension.js require，社平原为空（已补全）
- E. `docs/js/provinces/*.json` — 网页文档残留，社平原为元/年单位错（已纠正）
- F. `js/provinces/*.json` — 网页文档残留，同上

> 用户原有误解"一份云函数用、一份网页用"。澄清：root json 是校验基准；线上是云函数 js；js/、docs/js/ 是网页文档残留。

## 四、待办（截至归档）

1. 用户在能联网环境 `git push origin main`（执行环境连不上 github.com，token 已在 remote URL）。
2. 微信开发者工具重新上传 `calculate` 云函数（今日用户已重传，确认生效）。
3. 网页版重建（进行中，见 `web/` 目录）。

## 五、今日提交清单（本地，未 push）

```
57fbf2a sync: 31省配置副本全量对齐(以root口径为准)
6936725 fix: 江西2020-2026改用官方计发基数/全口径社平
1419504 fix: 江西2021-2026社平改用统计局官方非私营数据(÷12)
739281b fix: 按官方数据校正江西base_rates.prov计发基数
ed74502 fix: 按甘肃省人社厅官方数据校正1995-2025
1269a37 fix: 江西2024社平/甘肃1995社平 + 案例重标定 + verify --update
9648a83 fix: B2 弹性提前退休路径 ef50/fw50 分支缺失
5cf3326 fix: 报告页C9 Canvas2D迁移+屏显修复
42a3fb6 fix: 审计P0/P1 引擎/数据/支付链路修复
3df02a7 fix: 报告屏显三处回归(A1/A2/B1)
06f0828 fix: 报告页三处修正
13bbcbd feat: 报告页视觉重设计
fd666cb fix: 分享图 roundRect 报错修复
bc264d3 fix: 虚拟支付 goodsPrice 单位修正为分
40d2ced fix: 双基数省份 base_rates 重复key隐患修复
d3d9cab fix: 吉林基础养老金=0 + 虚拟支付签名缺失防御
f7487bf feat: 支付链路迁移到微信虚拟支付
c1f5d09 feat: 结果分享图改用原生Canvas 2D
4d602c4 fix: 移除 app.json 无效 keywords/description
```
