# 养老金计算平台 — 上线准备清单

> 生成日期：2026-06-26 08:34  
> 审核版本：v1.0.5（审核中，已第3天）  
> 代码版本：v2.0-dev（含付费报告、精确月表等）  
> 验证状态：✅ 172/172 全部通过

---

## 一、审核与发布状态

| 版本 | 内容 | 状态 |
|:----:|:----|:----:|
| **v1.0.5** | 免费基础版（含所有UI修复、隐私协议、数据修复） | **⏳ 审核中（6月23日提交，正常1-3天）** |
| **v2.0（未提审）** | 付费报告 + 精确月表 + 西藏高原补贴 | ⏸️ 待审核通过后发布 |

### v1.0.5 审核包含的内容
- 6步引导流程（省份→缴费→确认→结果）
- 31省最新社平工资 / 计发基数数据
- 隐私协议页面（`__usePrivacyCheck__: true`）
- 延迟退休计算（国办发〔2025〕5号）
- 弹性提前退休比较
- 分享功能（好友/朋友圈）

### v2.0 新增（未提审）
- ✅ 付费报告页（pages/report/，已有骨架）
- ✅ 引擎计发月数表升级为官方精确到月
- ✅ 西藏高原补贴
- ⏳ 支付集成（createOrder/payCallback 云函数待部署）
- ⏳ 微信支付授权（待管理员扫码）

---

## 二、上线前检查清单

### 🔴 必须项（上线前确认）

#### 2.1 审核通过后的操作
- [ ] 收到审核通过通知 → 在「版本管理」点击「发布」
- [ ] 发布后验证线上版本可用（搜索小程序名进入测试）
- [ ] 生成真实小程序码（替换占位图）

#### 2.2 云函数部署确认
- [x] **calculate 云函数**：已部署（含精确月精度表）
- [ ] **createOrder 云函数**：❌ 未部署到DevTools
  - 路径：`miniprogram/cloud-functions/createOrder/` 不存在
  - 需要从 `cloudfunctions/createOrder/` 拷贝到 DevTools 目录
- [ ] **payCallback 云函数**：❌ 未部署到DevTools
  - 路径：`miniprogram/cloud-functions/payCallback/` 不存在
  - 需要从 `cloudfunctions/payCallback/` 拷贝到 DevTools 目录

#### 2.3 支付集成（v2.0提审前必须完成）
- [ ] 管理员扫码授权云调用支付（当前显示「管理员未授权」）
- [ ] 将 `createOrder` / `payCallback` 部署到 DevTools
- [ ] 验证支付流程：result.js → 下单 → 调起支付 → 成功跳转报告

### 🟡 建议项

#### 2.4 数据质量
- [ ] 各省计发基数按官方文件逐省核实（已修复湖南数据）
- [ ] 深圳地区 avg_salary_history 数据写入

#### 2.5 体验优化
- [ ] 结果页分享图保存到相册（低优）
- [ ] iOS 兼容性测试（目前已完成Android真机测试）
- [ ] 小程序码替换为真实码（审核通过后生成）

---

## 三、上线后计划

### 阶段一：v1.0.5 发布上线（审核通过→当天发布）
1. 点击发布
2. 验证线上版本
3. 生成小程序码

### 阶段二：支付功能开发（v1.0.5 发布后启动）
1. 管理员扫码授权云调用支付
2. 部署 createOrder/payCallback 到 DevTools
3. 真机测试支付流程
4. 提审 v2.0（含付费报告+支付）

### 阶段三：持续优化
1. 关注审核反馈
2. 订阅消息接入
3. 数据持续维护

---

## 四、关键文件索引

| 文件 | 说明 |
|:----|:----|
| `miniprogram/` | 小程序前端代码（DevTools加载目录） |
| `miniprogram/cloud-functions/calculate/` | 计算云函数（已部署） |
| `cloudfunctions/createOrder/` | 支付下单云函数（待部署到DevTools） |
| `cloudfunctions/payCallback/` | 支付回调云函数（待部署到DevTools） |
| `engine/pension-engine.js` | 核心计算引擎 |
| `provinces/*.json` | 31省数据配置 |
| `cases/` | 172个测试案例 |
| `scripts/verify.js` | 验证脚本 |
| `docs/05-测试/` | 测试文档 |
| `docs/07-归档/` | 版本归档 |

---

## 五、提醒

- **审核周期**：1-3个工作日，第3天可留意通知
- **云函数同步**：工作区引擎改完后，需要用 cp 命令同步到 `miniprogram/cloud-functions/calculate/` 再在DevTools上传部署
- **支付云函数**：`createOrder` 和 `payCallback` 尚未部署到DevTools，v2.0提审前部署即可
