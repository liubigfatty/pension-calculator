# 缴费指数小程序 · 测试方案（可直接照填）

> 说明：以下所有「预期结果」均由引擎实跑得出，与 `scripts/verify-index-mini.js` 完全对齐。
> 省份统一选 **吉林省**；如换省，预期值会变（社平不同）。
> 使用前请确认已上传最新版 `calcIndex` 云函数（含 C+正向拦截修复，commit def340e）。

---

## 用例 1：A 详细 + 正向（冒烟测试，最推荐先试）

**操作**：颗粒度选 `A 详细` → 模式 `正向` → 把下面 JSON 粘到输入框 → 点计算

```json
[
  { "year": 2020, "months": 12, "baseAvg": 4980.5 },
  { "year": 2021, "months": 12, "baseAvg": 5450.3 },
  { "year": 2022, "months": 11, "baseAvg": 5890.1 }
]
```

**预期结果**：
| 项目 | 值 |
|------|------|
| 本人平均缴费工资指数 | **0.8787** |
| 个人账户余额（估算） | **¥16081.14** |
| 累计缴费 | 35 个月（2.92 年） |

---

## 用例 2：C 最简 + 反推（与用例1互验）

**操作**：颗粒度选 `C 最简` → 模式 `反推` → 填：
- 参加年份：`2020`
- 参加月份：`1`
- 累计月数：`35`
- 当前账户余额：`16081`

**预期结果**：
| 项目 | 值 |
|------|------|
| 推算平均指数 | **≈0.8772** |
| 收敛 | 是（计算余额≈¥16085，与输入余额误差很小） |

> 说明：反推假设「每年指数恒定」，所以 0.8772 与用例1正向的 0.8787 略有差异，属正常（互相印证即可）。

---

## 用例 3：B 中等 + 正向（验证 B 颗粒度结构）

**操作**：颗粒度选 `B 中等` → 模式 `正向` → 填：
- 参加年份：`2020`
- 累计月数：`35`
- 逐年数据（粘到 JSON 框）：
```json
[
  { "year": 2020, "months": 12, "baseAvg": 4980.5 },
  { "year": 2021, "months": 12, "baseAvg": 5450.3 },
  { "year": 2022, "months": 11, "baseAvg": 5890.1 }
]
```

**预期结果**：应与用例1完全一致
| 项目 | 值 |
|------|------|
| 本人平均缴费工资指数 | **0.8787** |
| 个人账户余额（估算） | **¥16081.14** |

---

## 用例 4：C 最简 + 正向（验证拦截修复）

**操作**：颗粒度选 `C 最简` → 模式 `正向` → 随便填参加年份/月数 → 点计算

**预期结果**：应弹出提示框（**不再出现全零结果**）
```
C 颗粒度（最简）只有起止时间和月数，没有缴费基数，无法正向计算指数。
请切换到"反推"模式（需填写账户余额），或改用 A/B 颗粒度输入缴费基数。
```

---

## 标杆用例：吉林真实样本（权威校准，295 个月）

> 来源：你提供的《个人缴费信息查询.xls》（吉林长春 1998–2022，295 个月真实逐月数据）。
> 这是验证引擎公式正确性的「金标准」，**数据量太大（296 行）不适合手工填**，由 `index-engine/test-calcIndex.js` 自动跑。

**A 详细 + 正向（脚本跑）**：
| 项目 | 值 |
|------|------|
| 平均指数 | **1.7836** |
| 账户余额 | **¥204822.97** |
| 1999 年指数 | 0.647（与人工校准报告精确匹配）|

**C 最简 + 反推（用上面余额反推，脚本跑）**：
| 项目 | 值 |
|------|------|
| 推算平均指数 | **≈1.9888** |
| 收敛 | 是 |

> 注意：C 反推的 1.9888 ≠ A 正向的 1.7836，原因同用例2——反推是「等效常数指数」，会把波动拉平。这是数学特性，不是 bug。

---

## 你截图那组的建议填法

你截图是：吉林 / C 最简 / 1998年7月 / 342个月 / 正向 → 全零。

**正确用法**（二选一）：
1. **想反推指数**：切到 `反推` 模式，在「当前账户余额」填你社保App里看到的真实余额（比如 69000 或 204822），即可得平均指数。
2. **想正向算指数**：切到 `A 详细` 或 `B 中等`，把每年的缴费基数填进去（从《个人缴费信息查询.xls》里每年取月均基数）。

---

## 判定标准

- 用例1/2/3 结果与上表一致 → 正向/反推/三颗粒度均正常 ✅
- 用例4 弹提示而非全零 → 拦截修复生效 ✅
- 任何用例出现 `NaN`、崩溃、或结果与预期差 >5% → 截图反馈，疑似数据/引擎问题 ❌
