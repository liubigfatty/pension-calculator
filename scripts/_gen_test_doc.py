# -*- coding: utf-8 -*-
import json
d = json.load(open('scripts/_xls_result.json', encoding='utf-8'))
std = d['std']; cur = d['current']
infer_std = d['infer_from_std']; infer_pri = d['infer_from_principal']
ma = d['monthly_avg']; pri = d['principal_sum']
sy, sm, tm = d['start_year'], d['start_month'], d['total_months']
yearly = d['yearly']

def pct(a, b):
    return (a - b) / b * 100

dev_cur = pct(cur['avgIndex'], std['avgIndex'])
dev_inf = pct(infer_std, std['avgIndex'])
dev_pri = pct(infer_pri, std['avgIndex'])

# 逐年明细表（按参加时间自动生成的完整行，含末行可能的空基数）
def gen_rows(sy, sm, tm):
    rows = []; rem = tm; y = sy; m = sm
    while rem > 0:
        mty = 13 - m; mm = min(rem, mty if mty > 0 else 12)
        rows.append((y, mm)); rem -= mm; y += 1; m = 1
    return rows
gr = gen_rows(sy, sm, tm)
base_by_year = {y['year']: y['baseAvg'] for y in yearly}
yrows = ''
for (yr, mm) in gr:
    b = base_by_year.get(yr)
    yrows += '| %d | %d | %s |\n' % (yr, mm, ('%.2f' % b) if b else '')
nrows = len(gr)

doc = f"""# 缴费指数小程序 · 真实数据测试方案

> 数据源：`个人缴费信息查询.xls`（295 条逐月缴费明细）
> 本方案用**最细颗粒度的真实数据**驱动统一表单的几种输入模式，给出可照填的测试用例与预期值，并揭示不同模式下的精度差异。

## 一、测试数据来源

| 项目 | 值 |
|------|-----|
| 参保地 | 吉林（长春，单位在中韩国际合作示范区） |
| 记录条数 | 295 条（逐月） |
| 缴费起止 | {sy}年{sm:02d}月 ~ 2022年11月 |
| 整段月均缴费基数 | {ma:.2f} 元 |
| 个人账户划入本金合计（不含息） | {pri:.2f} 元 |

> 注：EXCEL 中「养老保险缴费基数」为当月基数，「缴费划账户」= 基数×8%（个人部分）。早期基数仅 353 元，后期达 18533 元，说明该职工缴费基数随社平增长而大幅上升。

## 二、金标准（最细颗粒度：逐年明细正向）

把 295 条逐月记录按年聚合成 **25 年逐年明细**，正向计算（指数 = 年基数 ÷ 上年社平，余额按统一记账利率复利）：

- **平均指数 = {std['avgIndex']}**
- **估算余额（含息） = {std['balance']:.2f} 元**

这是信息最完整、最准的基准值，作为其他模式的对照锚点。

## 三、四种输入模式对照测试

| 模式 | 小程序填法 | 平均指数 | 余额(估算/反推) | 与金标准偏差 |
|------|-----------|---------|----------------|-------------|
| **A 金标准** | 展开「逐年明细」填 25 年 | **{std['avgIndex']}** | {std['balance']:.2f}（正向含息） | 基准 |
| **1 当前基数** | 当前基数填 {ma:.2f} | {cur['avgIndex']} | {cur['balance']:.2f}（正向含息） | {dev_cur:+.1f}%（估算，见四.3） |
| **2 余额(含息)** | 账户余额填 {std['balance']:.2f} | {infer_std}（反推） | — | {dev_inf:+.1f}% 接近 |
| **2b 余额(本金)** | 账户余额填 {pri:.2f} | {infer_pri}（反推） | — | {dev_pri:+.1f}% 偏低 |
| **3 两者都填** | 当前基数 + 余额都填 | forward {cur['avgIndex']} / infer {infer_std} | 对照提示 | — |

## 四、分析与结论

1. **最细模式（逐年明细）最准**：指数 {std['avgIndex']}，作为基准。适用于能导出逐年/逐月缴费基数的人。
2. **余额反推（填含息余额）接近基准**：指数 {infer_std}，偏差仅 {dev_inf:+.1f}%。反推假设"每年指数恒定"求等效常数，故略高于真实加权平均，属正常数学特性。适合"只知道账户余额"的用户。
3. **当前基数法（按现在工资估算）**：指数 {cur['avgIndex']}，偏差 {dev_cur:+.1f}%。
   - **原理**：你填的「当前月缴费基数」÷ **当前社平** = 当下指数，把它当作平均缴费指数，反推往年每年基数（= 当年社平 × 当下指数），再算账户余额（含息）。即"用现在的比例，代表过去所有年份"。
   - **本例为何偏低**：该职工真实缴费是"前低后高"（早期基数 353、后期 18533）。当前基数法假设全程按"现在这个比例（0.93）"缴，而真实加权 1.78 是被后期 2.85 的高比例拉高的。所以"只填现在工资"会**低估**历史高比例年份的贡献。
   - 这是"只知道现在工资"时最合理的估算；要想更准，请用「逐年明细」或「账户余额」。
   - ⚠️ 注意：这里填的应是你**现在的**月工资基数，不是历史平均值。本例用 EXCEL 的整段月均 {ma:.2f} 仅作演示，真实使用时请填你当前工资对应的缴费基数。
4. **本金和（不含息）偏低**：指数 {infer_pri}，偏差 {dev_pri:+.1f}%，因未含历年记账利息。填余额时应填社保 App 查到的"个人账户累计存储额"（含息），不是各年本金之和。

## 五、逐年明细（点开自动按参加时间排好，只填基数）

**操作**：先在基础信息填好「参加工作时间 = {sy}年{sm:02d}月」「累计缴费月数 = {tm}」，然后点开「我有某些年份的明细」——小程序会**自动从 {sy} 年排到缴费结束**（共 {nrows} 行），年份和月数都算好了，你只需在每行填当年的「月均缴费基数」。下表就是自动生成后的样子（年份/月数系统已填，你只照填基数那一列）：

| 年份 | 缴费月数（系统自动） | 月均基数（你填） |
|------|--------|------------------|
{yrows}

> 共 {nrows} 行（{sy}~2023），其中 25 个有数据年份，合计 {tm} 个月，与 EXCEL 记录数一致。

## 六、照填验证步骤

**验证金标准（推荐先做）**
1. 参保地选「吉林」
2. 参加时间：{sy}年{sm:02d}月；累计月数：{tm}
3. 先在基础信息填好「参加工作时间 {sy}年{sm:02d}月」「累计缴费月数 {tm}」，再点开「逐年明细」——系统自动生成 {nrows} 行（年份和月数已填好，从 {sy} 到 2023）。你只需按上方表格在每行填「月均基数」：{sy}填353、1999填353……2022填18532.95（有数据的年份填，末行 2023年1月 无数据留空）。**不用写 JSON**。
4. 点计算 → 预期：平均指数 **{std['avgIndex']}**，估算余额 **{std['balance']:.2f}**

**验证余额反推**
1. 参保地/参加时间/月数同上
2. 不展开逐年明细；账户余额填 **{std['balance']:.2f}**（金标准含息余额，或用你社保 App 真实查到的累计存储额）
3. 点计算 → 预期：反推平均指数 ≈ **{infer_std}**（与金标准偏差 < 15% 即正常）

**验证当前基数模式（按现在工资估算）**
1. 参保地/参加时间/月数同上
2. 当前月缴费基数填 **{ma:.2f}**（即你现在的月工资基数；本例用整段平均值演示）
3. 点计算 → 预期：平均指数 ≈ **{cur['avgIndex']}**，估算余额 ≈ **{cur['balance']:.2f}**（这是"假设全程按当前比例"的估算值，与金标准不同属正常，见四.3）

**验证拦截（无输入）**
1. 参保地/参加时间/月数填好，基数和余额都留空
2. 点计算 → 预期：提示"请至少填写月均基数或账户余额之一"
"""

import os
os.makedirs('docs/08-缴费指数小程序', exist_ok=True)
open('docs/08-缴费指数小程序/05-真实数据测试方案.md', 'w', encoding='utf-8').write(doc)
print('已生成 docs/08-缴费指数小程序/05-真实数据测试方案.md')
print('偏差: 当前基数%.1f%% 反推(含息)%.1f%% 反推(本金)%.1f%%' % (dev_cur, dev_inf, dev_pri))
