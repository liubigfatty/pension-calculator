# 补齐「平均缴费工资指数计算器」对正元引擎的影响评估

> 评估日期：2026-08-14
> 背景：上一轮「正元 vs 网上计算器深度评估」中，把"平均缴费指数需手输"列为第三层（数据真实性）最大短板。本文量化补齐该短板后的变化。

## 一、先说清楚：补的到底是哪块

引擎现状（已用源码坐实）：

- `pension-engine.js` L1401：`const avgIndex = parseFloat(inputData.avgIndex) || 1.0`
- 即**平均缴费工资指数是纯手输的点值**，用户不知道自己真实指数只能估，估不出来引擎默认填 `1.0`（偏高）。
- 双指数省（山西/天津/江苏）还有独立的 `transIndex`（过渡性养老金指数），L437–515 单独消费，缺了会 `console.warn` 告警误用（L448）。

**补的模块**：输入"历年缴费工资基数序列"（从建账年起每年一个数）→ 引擎用已有的 `AVG_SALARY_HISTORY`（历年社平，审计确认 31 省全表）→ 算出平均缴费指数并回填主计算。

平均缴费指数算法：
```
avg_index = Σ(当年缴费工资基数 ÷ 当年全省社平) ÷ 实际缴费年限
```

## 二、量化：现在"手输指数"这个短板的真实代价

用两个真实 verified 案例（仅含基础+过渡两项，不含个人账户余额本身对指数的间接影响）：

| 省 | 计发基数 | 指数每错 0.1 → 月养老金差 |
|---|---|---|
| 江西（低社平,verified） | 6916 | **约 129 元/月** |
| 海南（高社平,verified） | 8131 | **约 353 元/月** |

- 指数从 0.694 估成 1.0（很多人以为"我一直按100%交"）：江西月差 **+393 元**、海南月差 **+1066 元**。
- 方向：估高→待遇虚高（退休后发现少拿）；估低→白担心。
- 高社平/长年限省更敏感（海南 41.92 年、指数 1.8444）。

**结论**：这不是"估算误差小"的短板，是每月 100~350 元、年 1200~4200 元量级的偏差源。补齐价值显著。

## 三、补上后，三层保真度模型怎么变

| 层 | 补前 | 补后 | 变化 |
|---|---|---|---|
| **第一层·公式结构** | 11+ 省真实结构 | 不变 | 无影响（指数计算器不改公式） |
| **第二层·时间维度** | 记账利率年表复利 | 不变 | 无影响 |
| **第三层·数据真实性** | 指数**手输/估**（易错，默认1.0） | 指数**由真实缴费工资算**（逐年社平已是真值） | **显著提升**：消除每月百元级偏差源 |

- **对官方平台（国家社保/电子社保卡）**：官方赢在"自动拉真实缴费工资+真实指数、零手填"。补上指数计算器后，正元仍需用户**手输缴费工资序列**（但序列比单点指数更容易从社保APP/核定表拿到），所以与官方的"数据真实性"差距**大幅缩小但未归零**。
- **对第三方通用计算器**：第三方压根没有"历年缴费工资"入口，要么让你估一个指数、要么直接忽略指数细节。补上后正元在第三层**彻底碾压第三方**。

## 四、补上后的残留短板（必须诚实列出）

1. **缴费工资序列仍需手输**——这是补上后唯一剩下的"大门槛"。官方平台零手填仍赢。缓解法：让用户粘贴社保缴费记录/核定表历年基数（比估一个指数容易得多）。
2. **双指数省 trans_index 边界**：普通"平均缴费工资指数"只算**建账年起**的 `avg_index`（用于基础养老金）。而山西/天津/江苏的**过渡性养老金**用独立的 `trans_index`（建账前段，视同缴费期间）。补上 avg_index 计算器**不自动解决 trans_index**——这两个省仍需提示用户补建账前段指数，否则会触发引擎 L448 告警。广东不是双指数（用 a 值），不受影响。
3. **早期段精度债**：7 省（fujian/hunan/jiangxi/jilin/tianjin/yunnan/heilongjiang）1996–2019 计发基数用社平近似值占位——若用早期社平算指数，早年段精度非真值（影响极小，近年段已填真值）。
4. **断缴/补缴/视同段处理**：平均缴费工资指数算法需正确跳过视同缴费年（不计入分母但有自己规则）、处理补缴基数，否则算出的指数会与核定表不符。

## 五、定位变化一句话

补齐前：正元 = 公式/省域精度碾压，但"指数靠用户估"拖后腿。
补齐后：指数也变成"用真实缴费工资算"，**第三层数据真实性从短板变长板**，正元在"已/临退人员核验核定表、跨省比较"场景对第三方形成代差，对官方平台只剩"零手填"一项差距。

## 六、若实现，交互建议

- 输入：历年缴费工资基数数组（建账年起）+ 是否双指数省提示。
- 输出：算出 avg_index（及双指数省的 trans_index 分段），**一键回填**主计算器的 avgIndex 字段。
- 校验：用 cases/ 里的 verified 案例反推，确保算出的指数与核定表标注指数一致（质量闭环）。
- 边界提示：双指数省明确告知"过渡性养老金另需建账前段指数"。

---
*附：指数敏感度量化脚本 `_index_sensitivity.cjs`（江西/海南 verified 案例）。*
